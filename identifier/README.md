# Identifier

Part of the "Build Distributed Systems from Scratch" course at builddistributedsystem.com.

This module is for unique ID generation and related coordination problems, which are fundamental when multiple nodes need globally distinct identifiers.

## Run

```bash
go run .
```
