# Reactor

Part of the "Build Distributed Systems from Scratch" course at builddistributedsystem.com.

This module is a natural place for an event-driven core, where incoming events are handled through a reactor-style loop rather than simple direct calls.

## Run

```bash
go run .
```
